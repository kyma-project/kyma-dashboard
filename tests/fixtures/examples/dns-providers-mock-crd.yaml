kind: CustomResourceDefinition
apiVersion: apiextensions.k8s.io/v1
metadata:
  name: dnsproviders.dns.gardener.cloud
  labels:
    app.kubernetes.io/instance: dns-controller-manager
    app.kubernetes.io/name: gardener-extension-shoot-dns-service
    gardener.cloud/deletion-protected: 'true'
    resources.gardener.cloud/managed-by: gardener
spec:
  group: dns.gardener.cloud
  names:
    plural: dnsproviders
    singular: dnsprovider
    shortNames:
      - dnspr
    kind: DNSProvider
    listKind: DNSProviderList
  scope: Namespaced
  versions:
    - name: v1alpha1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          required:
            - spec
          properties:
            apiVersion:
              description: >-
                APIVersion defines the versioned schema of this representation
                of an object. Servers should convert recognized schemas to the
                latest internal value, and may reject unrecognized values. More
                info:
                https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: >-
                Kind is a string value representing the REST resource this
                object represents. Servers may infer this from the endpoint the
                client submits requests to. Cannot be updated. In CamelCase.
                More info:
                https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              type: object
              properties:
                defaultTTL:
                  description: default TTL used for DNS entries if not specified explicitly
                  type: integer
                  format: int64
                domains:
                  description: >-
                    desired selection of usable domains (by default all zones
                    and domains in those zones will be served)
                  type: object
                  properties:
                    exclude:
                      description: values that should be ignored (domains or zones)
                      type: array
                      items:
                        type: string
                    include:
                      description: values that should be observed (domains or zones)
                      type: array
                      items:
                        type: string
                providerConfig:
                  description: optional additional provider specific configuration values
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                secretRef:
                  description: >-
                    access credential for the external DNS system of the given
                    type
                  type: object
                  properties:
                    name:
                      description: >-
                        Name is unique within a namespace to reference a secret
                        resource.
                      type: string
                    namespace:
                      description: >-
                        Namespace defines the space within which the secret name
                        must be unique.
                      type: string
                type:
                  description: >-
                    type of the provider (selecting the responsible type of DNS
                    controller)
                  type: string
                zones:
                  description: >-
                    desired selection of usable domains the domain selection is
                    used for served zones, only (by default all zones will be
                    served)
                  type: object
                  properties:
                    exclude:
                      description: values that should be ignored (domains or zones)
                      type: array
                      items:
                        type: string
                    include:
                      description: values that should be observed (domains or zones)
                      type: array
                      items:
                        type: string
            status:
              type: object
              properties:
                defaultTTL:
                  description: actually used default TTL for DNS entries
                  type: integer
                  format: int64
                domains:
                  description: actually served domain selection
                  type: object
                  properties:
                    excluded:
                      description: Excluded values (domains or zones)
                      type: array
                      items:
                        type: string
                    included:
                      description: included values (domains or zones)
                      type: array
                      items:
                        type: string
                lastUpdateTime:
                  description: >-
                    lastUpdateTime contains the timestamp of the last status
                    update
                  type: string
                  format: date-time
                message:
                  description: >-
                    message describing the reason for the actual state of the
                    provider
                  type: string
                observedGeneration:
                  type: integer
                  format: int64
                state:
                  description: state of the provider
                  type: string
                zones:
                  description: actually served zones
                  type: object
                  properties:
                    excluded:
                      description: Excluded values (domains or zones)
                      type: array
                      items:
                        type: string
                    included:
                      description: included values (domains or zones)
                      type: array
                      items:
                        type: string
      subresources:
        status: {}
      additionalPrinterColumns:
        - name: TYPE
          type: string
          jsonPath: .spec.type
        - name: STATUS
          type: string
          jsonPath: .status.state
        - name: AGE
          type: date
          description: creation timestamp
          jsonPath: .metadata.creationTimestamp
        - name: INCLUDED_DOMAINS
          type: string
          description: included domains
          jsonPath: .status.domains.included
        - name: INCLUDED_ZONES
          type: string
          description: included zones
          priority: 2000
          jsonPath: .status.zones.included
        - name: MESSAGE
          type: string
          description: message describing the reason for the state
          priority: 2000
          jsonPath: .status.message
  conversion:
    strategy: None
status:
  conditions:
    - type: NamesAccepted
      status: 'True'
      lastTransitionTime: '2023-05-31T07:56:28Z'
      reason: NoConflicts
      message: no conflicts found
    - type: Established
      status: 'True'
      lastTransitionTime: '2023-05-31T07:56:28Z'
      reason: InitialNamesAccepted
      message: the initial names have been accepted
  acceptedNames:
    plural: dnsproviders
    singular: dnsprovider
    shortNames:
      - dnspr
    kind: DNSProvider
    listKind: DNSProviderList
  storedVersions:
    - v1alpha1
